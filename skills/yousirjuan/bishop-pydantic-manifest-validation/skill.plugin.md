# Bishop Pydantic manifest validation

Validate `manifest.json` and agent folders with `AgentManifest` + `AgentValidator` in Bishop `core/`.

**Invoke:** Use **bishop-pydantic-manifest-validation** when editing manifests or kingdom API models.

**Before:** bishop-agent-birth-kit (scaffold first).

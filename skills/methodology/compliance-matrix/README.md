# compliance-matrix

Per-stack / per-X compliance snapshots in markdown tables. Update on every change to ensure no stack drifts off the contract (label schema, frontmatter, doctor presence, etc.).

See [`SKILL.md`](SKILL.md) for the playbook and `docs/templates/integration-checklist.md.template` and `skills/infra/dockyard/references/integration-checklist.md` for the canonical reference.

---
name: compliance-matrix
description: Per-stack / per-X compliance snapshots in markdown tables. Update on every change to ensure no stack drifts off the contract (label schema, frontmatter, doctor presence, etc.). Triggers on "compliance check", "per-stack matrix", "which stacks have X". See `docs/standards/` and `docs/improvement/recurring-wins.md` for context. Pairs with the canonical reference: `docs/templates/integration-checklist.md.template` and `skills/infra/dockyard/references/integration-checklist.md`.
---

# compliance-matrix — methodology skill

Per-stack / per-X compliance snapshots in markdown tables. Update on every change to ensure no stack drifts off the contract (label schema, frontmatter, doctor presence, etc.).

## When to use

Triggers: "compliance check", "per-stack matrix", "which stacks have X".

## How

See the canonical reference: `docs/templates/integration-checklist.md.template` and `skills/infra/dockyard/references/integration-checklist.md`.

## Why this exists

Codified from the 2026-05-14 sovereign-stack session. Used repeatedly across SEEME, self-hosted-git, Homelab Console, and Dockyard integration. Logged in [`docs/improvement/recurring-wins.md`](../../../docs/improvement/recurring-wins.md).

## Pairing

- **Standard:** `docs/templates/integration-checklist.md.template` and `skills/infra/dockyard/references/integration-checklist.md`
- **Template (if applicable):** see `docs/templates/`
- **Checklist (if applicable):** see `docs/checklists/`
- **Drill (if applicable):** see `docs/training/drills/`

## Status

Stub. Body to be fleshed out as patterns are exercised in future sessions. Frontmatter is authoritative for agent triggering.

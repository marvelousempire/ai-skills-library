# ai-skills-library Operating Rules

- Treat this repo as the reusable skills library, not a shipped application.
- Prefer pointers and indexes over duplicating long skill bodies into downstream repos.
- Keep generated AI_AGENT_RULES summaries compact; do not copy secrets or full histories into prompt context.
- Preserve hand-authored binder pages. Refresh only missing or generated sections.
